services:
  web:
    build: .
    ports:
      - "80:80"
    environment:
      - APP_ENV=${APP_ENV:-production}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
